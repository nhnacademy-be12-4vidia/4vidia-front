spring:
  application:
    name: 4vidia-front

  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:10469}
      username: ${ADMIN_ID}
      password: ${ADMIN_PASSWORD}

  profiles:
    active: prod
